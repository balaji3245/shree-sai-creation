import Link from 'next/link';
import { ArrowLeft, Check, ShieldCheck } from 'lucide-react';

export function AuthLayout({ children, mode }: { children: React.ReactNode; mode: 'login' | 'signup' }) {
  const isLogin = mode === 'login';
  return <main className="min-h-[calc(100svh-76px)] bg-ivory"><div className="grid min-h-[calc(100svh-76px)] lg:grid-cols-2"><section className="relative hidden overflow-hidden bg-ink p-12 text-ivory lg:flex lg:flex-col"><img src="https://images.unsplash.com/photo-1540932239986-30128078f3c5?auto=format&fit=crop&w=1600&q=85" alt="Luxury chandelier in a refined interior" className="absolute inset-0 h-full w-full object-cover opacity-40"/><div className="absolute inset-0 bg-gradient-to-b from-black/75 to-black/35"/><div className="relative"><p className="eyebrow">Shree Sai Creation</p><h1 className="display mt-5 max-w-lg text-7xl">Where exceptional spaces find their light.</h1></div><div className="relative mt-auto"><p className="max-w-md text-sm leading-7 text-stone/85">Save your favourite pieces, send considered enquiries and make your next lighting project easier to shape.</p><div className="mt-8 space-y-3 text-xs text-stone/85">{['Save and revisit your preferred pieces','Manage your design consultation requests','Receive tailored collection updates'].map(item => <p className="flex items-center gap-3" key={item}><Check size={15} className="text-champagne"/>{item}</p>)}</div></div></section><section className="flex items-center justify-center px-5 py-14 sm:px-10"><div className="w-full max-w-md"><Link href="/" className="inline-flex items-center gap-2 text-[10px] font-bold uppercase tracking-[.14em] text-ink/55 transition hover:text-champagne"><ArrowLeft size={14}/> Back to website</Link><div className="mt-12">{children}</div><p className="mt-10 flex items-center gap-2 border-t border-stone pt-6 text-[10px] leading-5 text-ink/45"><ShieldCheck size={15} className="text-champagne"/> Your details are kept private and secure.</p><p className="mt-4 text-xs text-ink/55">{isLogin ? "New to Shree Sai Creation?" : 'Already have an account?'} <Link href={isLogin ? '/signup' : '/login'} className="font-bold text-champagne hover:text-bronze">{isLogin ? 'Create an account' : 'Sign in'}</Link></p></div></section></div></main>;
}
