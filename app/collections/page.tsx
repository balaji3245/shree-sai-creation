'use client';
import { useMemo, useState } from 'react'; import { Search } from 'lucide-react'; import { products, categories } from '@/data/site-data'; import { PageHero } from '@/components/shared/page-hero'; import { ProductCard } from '@/components/shared/ui';
export default function Collections(){const [term,setTerm]=useState('');const [category,setCategory]=useState('All'); const shown=useMemo(()=>products.filter(p=>(category==='All'||p.category===category)&&p.name.toLowerCase().includes(term.toLowerCase())),[term,category]);return <main><PageHero eyebrow="The collection" title="Considered objects of light." copy="Explore chandeliers and decorative lighting defined by material, proportion and a distinctly personal glow."/><section className="container-luxe py-12"><div className="flex flex-col justify-between gap-6 border-y border-stone py-5 lg:flex-row lg:items-center"><div className="flex gap-2 overflow-x-auto">{['All',...categories.map(c=>c.name)].map(c=><button key={c} onClick={()=>setCategory(c)} className={`whitespace-nowrap px-3 py-2 text-[10px] font-bold uppercase tracking-[.12em] ${category===c?'bg-ink text-ivory':'border border-stone text-ink/60'}`}>{c}</button>)}</div><label className="flex items-center gap-2 border-b border-ink/30 pb-2 text-sm"><Search size={15}/><input value={term} onChange={e=>setTerm(e.target.value)} placeholder="Search pieces" className="w-44 bg-transparent outline-none placeholder:text-ink/40"/></label></div><div className="mt-10 flex items-center justify-between"><p className="text-xs text-ink/55">{shown.length} pieces</p><button className="text-xs font-bold uppercase tracking-[.12em]">Sort: Featured</button></div><div className="mt-8 grid gap-x-5 gap-y-10 sm:grid-cols-2 lg:grid-cols-3 xl:grid-cols-4">{shown.map(p=><ProductCard key={p.id} product={p}/>)}</div>{!shown.length&&<p className="py-20 text-center text-ink/50">No pieces matched your search. Explore all collections.</p>}</section></main>}
